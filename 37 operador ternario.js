// EL operador ternario en JS es una forma corta y elegante de una estructura if
//if ...else

// se llama ternario porque tiene tres operadores, observemos la sintaxis
// condicion ? ValorSiverdadero : valorSiFalso

// ejemplo 1

let age = 18;
let message = age >= 18 ? "Eres mayor de edad" : "Eres menor de edad"
console.log(message);